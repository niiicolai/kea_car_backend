{
    "User max must be less than 20": { 
        "%User": { "max": { "<": 20 } }
    },
    "Nice max must be less than 20": { 
        "%Nice": { "max": { "<": 20 } }
    },
    "System max must be less than 20": {
        "%System": { "max": { "<": 20 } }
    },
    "iowait max must be less than 20": { 
        "%IOWait": { "max": { "<": 20 } }
    },
    "steal max must be less than 20": { 
        "%Steal": { "max": { "<": 20 } }
    },
    "idle max must be less than or equal 100": {
        "%Idle": { "max": { "<=": 100 } }
    }
}
